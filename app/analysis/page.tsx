export default function AnalysisPage() {
  return (
    <main className="mx-auto max-w-5xl p-6">
      <div className="panel p-6">
        <h1 className="font-[var(--font-grotesk)] text-2xl font-semibold">Advanced Analysis</h1>
        <p className="mt-2 text-sm text-terminal-dim">Technical + macro + financial + sentiment signals are blended with weighted scoring.</p>
      </div>
    </main>
  );
}
